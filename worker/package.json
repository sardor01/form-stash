{
  "name": "form-stash-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare-config": "node scripts/render-wrangler.mjs",
    "predeploy": "pnpm prepare-config",
    "predev": "pnpm prepare-config",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.50.0"
  }
}
